# name: Update Spotify Tracks

# on:
#   # Schedule the job to run every 2 hours
#   schedule:
#     - cron: "0 */2 * * *"

#   # Add workflow_dispatch to manually trigger the workflow
#   workflow_dispatch:

# jobs:
#   update-tracks:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v2

#       - name: Set up Python
#         uses: actions/setup-python@v2
#         with:
#           python-version: "3.x"

#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install pymongo spotipy python-dotenv  # Your dependencies

#       - name: Run Track Update Script
#         run: |
#           python app/jobs/maintain_tracks.py

name: Update Spotify Tracks

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

jobs:
  update-tracks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pymongo spotipy python-dotenv

      - name: Run Track Update Script
        env:
          SPOTIPY_CLIENT_ID: ${{ secrets.SPOTIPY_CLIENT_ID }}
          SPOTIPY_CLIENT_SECRET: ${{ secrets.SPOTIPY_CLIENT_SECRET }}
          SPOTIFY_CLIENT_ID: ${{ secrets.SPOTIFY_CLIENT_ID }}
          SPOTIFY_CLIENT_SECRET: ${{ secrets.SPOTIFY_CLIENT_SECRET }}
          MONGO_URL: ${{ secrets.MONGO_URL }}
        run: |
          python app/jobs/maintain_tracks.py
